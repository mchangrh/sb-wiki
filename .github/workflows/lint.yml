name: lint
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: StephaneBour/actions-php-cs-fixer@8.2
        with:
          dir: './mediawiki/'